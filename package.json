{
  "name": "hangtola",
  "private": true,
  "description": "夯到拉排行榜：多模态 Agent Skill + 单文件 tier list 编辑器",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "check": "node scripts/check.mjs",
    "deploy": "npm run build && wrangler deploy"
  }
}
