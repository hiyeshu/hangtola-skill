# scripts/
> L2 | 父级: ../CLAUDE.md

成员清单

render-board.js: Node.js ESM 零依赖渲染 CLI，归一化 schema、嵌入本地图片并安全生成离线 HTML；扩展名兼容 RedSkill，模块语义由上级 package.json 固定。

确定性操作优先收敛到此目录；新增脚本必须实际运行验证。
[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
